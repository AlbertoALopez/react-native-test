/**
 * A simple weather display application
 * @flow
 */

import { AppRegistry } from 'react-native';
import WeatherApp from './WeatherApp.js';

AppRegistry.registerComponent('WeatherApp', () => WeatherAp);
